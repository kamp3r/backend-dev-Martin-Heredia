{{#if prod.length }}
<h2 class="text-success mt-4">Productos cargados</h2>  
<table class="table text-success" id="tableProd">
        <thead>
          <tr>
            <th scope="col">#</th>
            <th scope="col">Título</th>
            <th scope="col">Precio</th>
            <th scope="col">Imagen</th>
          </tr>
        </thead>
        <tbody>
          {{#each prod}}
              <tr>
                  <th scope="row">{{this.id}}</th>
                  <td>{{this.title}}</td>
                  <td>${{this.price}}</td>
                  <td><img src="{{this.thumbnail}}" alt="{{this.thumbnail}}" width="150"></td>
                </tr>
          {{/each}}      
        </tbody>
      </table>
{{/if}}